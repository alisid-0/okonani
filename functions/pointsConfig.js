/** 1 point per $1 spent (after discounts). */
const POINTS_PER_DOLLAR = 1

/** Redeem 100 points for a one-time Stripe coupon. */
const REDEEM_POINTS_COST = 100
const REDEEM_DISCOUNT_CENTS = 500

module.exports = {
  POINTS_PER_DOLLAR,
  REDEEM_POINTS_COST,
  REDEEM_DISCOUNT_CENTS,
}
